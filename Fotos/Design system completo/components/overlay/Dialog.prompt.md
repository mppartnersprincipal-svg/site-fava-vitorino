Modal com véu `rgba(8,50,36,.55)` e fio dourado sob o título.

```jsx
<Dialog open={open} onClose={close} title="Agendar conversa" footer={<Button variant="gold">Enviar</Button>}>
  <Input label="Nome" />
</Dialog>
```
