Checkbox (check dourado sobre verde) e Radio (ponto verde).

```jsx
<Checkbox checked={ok} onChange={setOk} label="Li e concordo com a política de privacidade" />
<Radio name="tipo" value="pf" checked={tipo === 'pf'} onChange={setTipo} label="Pessoa física" />
```
