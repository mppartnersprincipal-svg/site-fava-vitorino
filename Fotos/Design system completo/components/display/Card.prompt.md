Superfície de card com cantos quase retos e borda sutil; sombra só no hover quando `interactive`.

```jsx
<Card interactive>
  <h3>Direito Imobiliário</h3>
  <p>Da compra à regularização.</p>
</Card>
```

Tons: `light` (padrão), `dark` (dentro de seções verdes), `sand`.
